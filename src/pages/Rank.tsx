import { RankGraph, RankHeader, RankSearchBar } from '@/components'

function Rank() {
  return (
    <nav>
      <RankHeader />
      <RankSearchBar />
      <RankGraph />
    </nav>
  )
}

export default Rank
