import { Character } from '.'

export interface Rank {
  beforeRank: number
  clanName: string
  grade: number
  nickname: string
  playerId: string
  rank: number
  ratingPoint: number
  represent: Character
}

// interface Rank {}
